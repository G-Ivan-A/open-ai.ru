# Руководство для контрибьюторов

Спасибо за интерес к проекту Open-AI.ru! Мы приветствуем вклад от всех участников сообщества.

## Как начать

### 1. Форк и клонирование

```bash
# Форк репозитория через GitHub UI
# Затем клонирование вашего форка
git clone https://github.com/ваш-username/open-ai.ru.git
cd open-ai.ru

# Добавление upstream remote
git remote add upstream https://github.com/G-Ivan-A/open-ai.ru.git
```

### 2. Настройка окружения

```bash
# Установка зависимостей
npm install

# Копирование .env файла
cp .env.example .env

# Редактирование .env с вашими настройками
```

### 3. Запуск в режиме разработки

```bash
# Через Docker Compose (рекомендуется)
docker-compose up -d

# Или локально
# Терминал 1 - Backend
cd apps/backend
npm install
npm run start:dev

# Терминал 2 - Frontend
cd apps/frontend
npm install
npm run dev
```

## Рабочий процесс

### 1. Создание issue

Перед началом работы:
- Проверьте существующие issues
- Создайте новый issue с описанием задачи
- Дождитесь обсуждения и одобрения (для крупных изменений)

### 2. Создание ветки

```bash
# Обновите main
git checkout main
git pull upstream main

# Создайте feature-ветку
git checkout -b feature/краткое-описание
# или
git checkout -b fix/описание-бага
```

Типы веток:
- `feature/` - новая функциональность
- `fix/` - исправление багов
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

### 3. Разработка

Следуйте этим принципам:

**Код:**
- TypeScript в strict mode
- Функциональный стиль где возможно
- SOLID принципы
- DRY (Don't Repeat Yourself)

**Комментарии:**
- На русском языке для бизнес-логики
- На английском для технических деталей (опционально)
- JSDoc для публичных API

**Форматирование:**
- ESLint + Prettier (автоматически при коммите)
- 2 пробела для отступов
- Single quotes для строк

### 4. Тестирование

```bash
# Запуск всех тестов
npm test

# Unit тесты
npm run test:unit

# E2E тесты
npm run test:e2e

# Линтинг
npm run lint

# Форматирование
npm run format
```

**Требования:**
- Минимум 80% покрытие для нового кода
- Все тесты должны проходить
- Нет ошибок линтера

### 5. Коммиты

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# Формат
<type>(<scope>): <subject>

# Примеры
feat(auth): добавить OAuth2 аутентификацию
fix(api): исправить ошибку валидации email
docs(readme): обновить инструкции по установке
refactor(database): оптимизировать запросы к БД
test(users): добавить unit тесты для UserService
```

**Типы коммитов:**
- `feat` - новая функциональность
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отступы
- `refactor` - рефакторинг без изменения функциональности
- `perf` - улучшение производительности
- `test` - добавление или обновление тестов
- `chore` - обновление зависимостей, конфигов
- `ci` - изменения CI/CD
- `build` - изменения в build процессе

**Пример детального коммита:**
```
feat(solutions): добавить фильтрацию каталога по категориям

- Реализован компонент CategoryFilter
- Добавлен API endpoint GET /solutions?category=:id
- Обновлена схема БД для индексации категорий
- Добавлены unit тесты для фильтрации

Closes #123
```

### 6. Push и Pull Request

```bash
# Push в ваш fork
git push origin feature/ваша-ветка

# Создайте Pull Request через GitHub UI
```

**Шаблон PR:**

```markdown
## Описание
Краткое описание изменений

## Связанные issue
Fixes #123

## Тип изменений
- [ ] Bug fix (исправление бага)
- [ ] New feature (новая функциональность)
- [ ] Breaking change (изменения с breaking API)
- [ ] Documentation update (обновление документации)

## Чеклист
- [ ] Код следует стилю проекта
- [ ] Проведен self-review
- [ ] Добавлены комментарии для сложной логики
- [ ] Обновлена документация
- [ ] Изменения не генерируют новых warnings
- [ ] Добавлены тесты
- [ ] Все новые и существующие тесты проходят
- [ ] Зависимые изменения были смержены

## Скриншоты (если применимо)
```

### 7. Code Review

- Будьте открыты к обратной связи
- Отвечайте на комментарии
- Вносите запрошенные изменения
- Минимум 1 approve для merge

## Соглашения по коду

### TypeScript

```typescript
// ✅ Хорошо
interface User {
  id: string;
  email: string;
  name: string;
}

const getUser = async (id: string): Promise<User> => {
  // Логика получения пользователя
};

// ❌ Плохо
const getUser = async (id: any) => {
  // any избегаем
};
```

### Именование

```typescript
// Переменные и функции - camelCase
const userName = 'Ivan';
const getUserById = (id: string) => { };

// Компоненты и классы - PascalCase
class UserService { }
const UserProfile = () => { };

// Константы - UPPER_SNAKE_CASE
const API_BASE_URL = 'https://api.example.com';
const MAX_RETRY_COUNT = 3;

// Файлы и папки - kebab-case
// user-profile.tsx
// api-client.ts

// Интерфейсы - префикс I
interface IUserRepository { }

// Типы - префикс T
type TUserRole = 'admin' | 'user';

// Enums - префикс E
enum EUserStatus {
  Active = 'ACTIVE',
  Inactive = 'INACTIVE',
}
```

### Структура файлов

```typescript
// 1. Imports
import { Injectable } from '@nestjs/common';
import { User } from './user.entity';

// 2. Types/Interfaces
interface CreateUserDto {
  email: string;
  name: string;
}

// 3. Constants
const DEFAULT_PAGE_SIZE = 20;

// 4. Main code
@Injectable()
export class UserService {
  // ...
}

// 5. Exports
export { CreateUserDto };
```

### React Components

```typescript
// ✅ Хорошо - Функциональный компонент с TypeScript
interface UserCardProps {
  user: User;
  onEdit: (user: User) => void;
}

export const UserCard: React.FC<UserCardProps> = ({ user, onEdit }) => {
  return (
    <div className="user-card">
      <h3>{user.name}</h3>
      <button onClick={() => onEdit(user)}>Редактировать</button>
    </div>
  );
};
```

### API Design

```typescript
// ✅ RESTful endpoints
GET    /api/users          # Список пользователей
GET    /api/users/:id      # Получить пользователя
POST   /api/users          # Создать пользователя
PUT    /api/users/:id      # Обновить пользователя
DELETE /api/users/:id      # Удалить пользователя

// ✅ GraphQL queries
query GetUser($id: ID!) {
  user(id: $id) {
    id
    name
    email
  }
}
```

## Тестирование

### Unit тесты

```typescript
// user.service.spec.ts
describe('UserService', () => {
  let service: UserService;

  beforeEach(() => {
    service = new UserService();
  });

  describe('getUser', () => {
    it('должен вернуть пользователя по id', async () => {
      const userId = '123';
      const result = await service.getUser(userId);

      expect(result).toBeDefined();
      expect(result.id).toBe(userId);
    });

    it('должен выбросить ошибку если пользователь не найден', async () => {
      await expect(service.getUser('invalid')).rejects.toThrow();
    });
  });
});
```

### E2E тесты

```typescript
// user.e2e.spec.ts
describe('User API', () => {
  it('GET /users - должен вернуть список пользователей', async () => {
    const response = await request(app.getHttpServer())
      .get('/users')
      .expect(200);

    expect(response.body).toHaveLength(10);
  });
});
```

## Документация

### JSDoc комментарии

```typescript
/**
 * Получает пользователя по ID
 *
 * @param id - Уникальный идентификатор пользователя
 * @returns Promise с объектом пользователя
 * @throws {NotFoundException} Если пользователь не найден
 *
 * @example
 * ```typescript
 * const user = await getUser('123');
 * console.log(user.name);
 * ```
 */
const getUser = async (id: string): Promise<User> => {
  // Реализация
};
```

### README для модулей

Каждый крупный модуль должен иметь README:

```markdown
# User Module

## Описание
Модуль управления пользователями

## API
- `GET /users` - список пользователей
- `POST /users` - создать пользователя

## Использование
...
```

## Безопасность

### Отчет об уязвимостях

**НЕ создавайте публичные issues для уязвимостей!**

Отправьте email на: security@open-ai.ru

Включите:
- Описание уязвимости
- Шаги для воспроизведения
- Потенциальное влияние
- Предложенное решение (опционально)

### Лучшие практики

- Никогда не коммитьте секреты, ключи, пароли
- Используйте `.env` для конфиденциальных данных
- Валидируйте пользовательский ввод
- Используйте prepared statements (через Prisma)
- Проверяйте зависимости на уязвимости: `npm audit`

## Коммуникация

- **GitHub Issues** - баги и фичи
- **GitHub Discussions** - вопросы и обсуждения
- **Telegram** - @openai_ru (быстрые вопросы)
- **Email** - info@open-ai.ru

## Лицензия

Внося свой вклад, вы соглашаетесь, что ваш код будет лицензирован под [MIT License](LICENSE).

## Благодарности

Спасибо всем контрибьюторам проекта! 🎉

---

## Дополнительные ресурсы

- [Документация Next.js](https://nextjs.org/docs)
- [Документация NestJS](https://docs.nestjs.com/)
- [Prisma Docs](https://www.prisma.io/docs)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Conventional Commits](https://www.conventionalcommits.org/)

Если у вас есть вопросы, не стесняйтесь спрашивать в GitHub Discussions!
